name: C/C++ CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: [ubuntu-latest,windows-latest]

    steps:
    - uses: actions/checkout@v3
    - name: make
      run: make
    - name: make test
      run: make test
    - name: run test
      run: ./test.app
    - name: clean
    - run: make clean
